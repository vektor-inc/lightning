<?php
/**
 * Backward-compat stub.
 *
 * The `VK_Custom_Text_Control` class has been migrated to the
 * composer-managed package `vektor-inc/vk-helpers` (loaded via
 * `vendor/autoload.php` in `functions.php`). It is now auto-declared on the
 * `customize_register` hook (priority 0) by the vendor package, so no
 * explicit require is needed from the theme. This stub file remains so
 * that legacy user code that `require`s this exact path does not produce
 * a fatal error.
 *
 * VK_Custom_Text_Control クラスは composer 管理の vektor-inc/vk-helpers に
 * 移行済み（functions.php の vendor/autoload.php で読み込まれる）。vendor
 * 側で customize_register フック（priority 0）で自動宣言されるため、テーマ
 * 側からの明示的な require は不要。ユーザー独自コードがこのパスを直接
 * require している場合に Fatal Error とならないように、空スタブとして
 * ファイル自体は残す。
 *
 * @package Lightning
 * @see https://github.com/vektor-inc/lightning/issues/1343
 */
